version: "2"
sql:
  - engine: "postgresql"
    queries: "queries/"
    schema: "schemas/"
    gen:
      go:
        package: "db"
        out: "../db"
        emit_json_tags: true
        emit_empty_slices: true
